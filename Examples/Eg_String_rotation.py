
'''reversedword=""
if len(word)%2==0:

    for char in reversed(word[0,2]):
         reversedword+=char
    for chr in word[]

        '''
def rotate_string(s,n):
    if len(s)!=len(n):
      
      return False
    
    combined=s+s
    return n in combined
    
s1 = input("Enter first string: ")
s2 = input("Enter second string: ")
result = rotate_string(s1, s2)
print(f"Is '{s2}' a rotation of '{s1}'? {result}")  
