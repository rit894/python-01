server_loads=[10,5,2,8]
new_tasks=int(input())
def distribute_tasks(server_loads,new_tasks):
    list1=[]
    for i in range(new_tasks):
        x=min(server_loads)
        y=min(server_loads)+1
        


       
       

    
print(distribute_tasks(server_loads,new_tasks))

